server:
  port: 9090
spring:
  datasource:
    username: root 
    password: root
    url: jdbc:mysql://localhost:3306/prueba?useSSL=false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL5Dialect